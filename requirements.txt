numpy>=1.26
pandas>=2.0
matplotlib>=3.8
scipy>=1.11
gguf>=0.10
huggingface_hub>=0.24
# Optional. Only needed on the discrete-GPU machine, to measure real device
# bandwidth and FLOPS. Everything else runs without it.
#   Windows + RTX 5080 (Blackwell, sm_120) needs a cu128 build:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# torch>=2.7
